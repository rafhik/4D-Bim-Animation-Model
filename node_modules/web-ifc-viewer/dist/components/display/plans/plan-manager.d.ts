import { Mesh, Vector3 } from 'three';
import { IfcPlane } from 'web-ifc-viewer/dist/components/display/clipping-planes/planes';
import { IfcClipper } from 'web-ifc-viewer/dist/components/display/clipping-planes/clipper';
import { Context } from 'web-ifc-viewer/dist/base-types';
import { IfcManager } from 'web-ifc-viewer/dist/components/ifc';
export interface PlanViewConfig {
    name: string;
    camera: Vector3;
    target: Vector3;
    normal?: Vector3;
    point?: Vector3;
    ortho?: boolean;
    rotation?: number;
}
export interface PlanView extends PlanViewConfig {
    plane?: IfcPlane;
}
export declare class PlanManager {
    private ifc;
    private context;
    private clipper;
    plans: {
        [name: string]: PlanView;
    };
    sectionFill: Mesh;
    active: boolean;
    currentPlan?: PlanView;
    private previousCamera;
    private previousTarget;
    private previousProjection;
    private storeys;
    constructor(ifc: IfcManager, context: Context, clipper: IfcClipper);
    getAll(): string[];
    create(config: PlanViewConfig): void;
    goTo(name: string, animate?: boolean): Promise<void>;
    exitPlanView(animate?: boolean): void;
    computeAllPlanViews(modelID: number): Promise<void>;
}
