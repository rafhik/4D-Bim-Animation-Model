import { Vector3, Intersection } from 'three';
import { IfcComponent, Context } from 'web-ifc-viewer/dist/base-types';
import { IfcPlane } from 'web-ifc-viewer/dist/components/display/clipping-planes/planes';
export declare class IfcClipper extends IfcComponent {
    dragging: boolean;
    planes: IfcPlane[];
    intersection: Intersection | undefined;
    orthogonalY: boolean;
    toleranceOrthogonalY: number;
    planeSize: number;
    private enabled;
    private readonly context;
    constructor(context: Context);
    get active(): boolean;
    set active(state: boolean);
    createPlane: () => void;
    createFromNormalAndCoplanarPoint: (normal: Vector3, point: Vector3) => IfcPlane;
    deletePlane: (plane?: IfcPlane | undefined) => void;
    deleteAllPlanes: () => void;
    setPlaneActive(plane: IfcPlane, active: boolean): void;
    private pickPlane;
    private createPlaneFromIntersection;
    private normalizePlaneDirectionY;
    private newPlane;
    private activateDragging;
    private deactivateDragging;
    private updateMaterials;
    private updateMaterial;
}
